fastapi==0.109.0
uvicorn==0.27.0
pydantic==2.6.0
pydantic-settings==2.1.0
httpx==0.26.0
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
# SDKs (Mocked or Real)
# Nillion secret vault SDK (examples repo: https://github.com/NillionNetwork/secretvaults-py)
secretvaults-py>=0.1.0  # install from PyPI or from the repo if not published
# NEAR helpers / relayer interaction (if you want a Python SDK)
near-api-py>=0.3.0
# Zecwallet wrapper (lightweight python wrapper around zecwallet-cli)
zecwallet>=0.0.5

# Note: If a package above is not available on PyPI, install from git or the example repo.
# Example: pip install git+https://github.com/NillionNetwork/secretvaults-py.git
